<Window x:Class="IMOS.PriceUpdater.Views.ValidationDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="CSV Validation" 
        Height="550" 
        Width="700"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="CSV File Validation" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- File Selection -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" Text="{Binding CsvFilePath}" 
                     VerticalAlignment="Center" Padding="5"/>
            <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}" 
                    Padding="15,5" Margin="5,0,0,0"/>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Validate" Command="{Binding ValidateCommand}" 
                    Padding="20,5" Margin="0,0,5,0"/>
            <Button Content="Clear" Command="{Binding ClearCommand}" 
                    Padding="15,5"/>
        </StackPanel>

        <!-- Validation Results -->
        <TabControl Grid.Row="3" Margin="0,0,0,10">
            <!-- Summary Tab -->
            <TabItem Header="Summary">
                <StackPanel Margin="10">
                    <Border Padding="10" Margin="0,0,0,10">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="Gray"/>
                                <Setter Property="BorderThickness" Value="1"/>
                                <Setter Property="CornerRadius" Value="5"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsValid}" Value="True">
                                        <Setter Property="Background" Value="#E8F5E9"/>
                                        <Setter Property="BorderBrush" Value="Green"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsValid}" Value="False">
                                        <Setter Property="Background" Value="#FFEBEE"/>
                                        <Setter Property="BorderBrush" Value="Red"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel>
                            <TextBlock FontSize="14" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="VALIDATION FAILED"/>
                                        <Setter Property="Foreground" Value="Red"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsValid}" Value="True">
                                                <Setter Property="Text" Value="VALIDATION PASSED"/>
                                                <Setter Property="Foreground" Value="Green"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="{Binding StatusMessage}" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Border>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding TotalRows}" FontSize="24" FontWeight="Bold" 
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="Total Rows" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding ValidRows}" FontSize="24" FontWeight="Bold" 
                                       HorizontalAlignment="Center" Foreground="Green"/>
                            <TextBlock Text="Valid Rows" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding ErrorCount}" FontSize="24" FontWeight="Bold" 
                                       HorizontalAlignment="Center" Foreground="Red"/>
                            <TextBlock Text="Errors" HorizontalAlignment="Center"/>
                        </StackPanel>

                        <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                            <TextBlock Text="{Binding WarningCount}" FontSize="24" FontWeight="Bold" 
                                       HorizontalAlignment="Center" Foreground="Orange"/>
                            <TextBlock Text="Warnings" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </TabItem>

            <!-- Errors Tab -->
            <TabItem Header="Errors">
                <DataGrid ItemsSource="{Binding Errors}" 
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Row" Binding="{Binding RowNumber}" Width="60"/>
                        <DataGridTextColumn Header="Column" Binding="{Binding ColumnName}" Width="120"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="100"/>
                        <DataGridTextColumn Header="Error Type" Binding="{Binding ErrorType}" Width="120"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <!-- Warnings Tab -->
            <TabItem Header="Warnings">
                <DataGrid ItemsSource="{Binding Warnings}" 
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Margin="5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Row" Binding="{Binding RowNumber}" Width="60"/>
                        <DataGridTextColumn Header="Column" Binding="{Binding ColumnName}" Width="120"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="100"/>
                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>

        <!-- Status Bar -->
        <StatusBar Grid.Row="5">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>

        <!-- Loading Overlay -->
        <Grid Grid.Row="0" Grid.RowSpan="6" 
              Visibility="{Binding IsValidating, Converter={StaticResource BooleanToVisibilityConverter}}"
              Background="#80000000">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Validating..." FontSize="16" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
